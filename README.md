# Michael David

Flutter Web personal site.

## Routes

- `/` — homepage
- `/tech` — technology
- `/admin` — administration
- `/real-estate` — real estate

## Run

```bash
cd ~/Desktop/projects/michael_david
flutter run -d chrome
```

Edit contact details and content in `lib/main.dart` (`SiteConfig`).
